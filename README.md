# Python 学習記録

SES待機期間中の自己学習リポジトリです。
案件参画時にスムーズに適応できるよう、実践的なスキル習得を目指します。

### Pythonの命名規則（PEP 8）
Pythonには「PEP 8」と呼ばれる公式のコーディング規約があり、誰が読んでも分かりやすいコードにするための明確な命名ルールが存在する。
（※ルールから外れてもエラーにはならないが、Pythonコミュニティの共通言語として守るのが基本）

- **パッケージ・モジュール**
  - ルール: すべて小文字。できるだけ短くする。
  - 例: `mypackage`, `date_utils.py`

- **クラス**
  - ルール: CapWords（パスカルケース）。各単語の先頭を大文字にし、アンダースコアは使わない。
  - 例: `UserProfile`, `MyClass`

- **変数・関数（メソッド）**
  - ルール: スネークケース。すべて小文字にし、単語の区切りにアンダースコア（`_`）を使う。
  - 例: `user_name`, `calculate_total()`

- **定数**
  - ルール: アッパースネークケース。すべて大文字にし、単語の区切りにアンダースコアを使う。
  - 例: `MAX_COUNT`, `TAX_RATE`

## 毎日の学習ログ

### 2026-09-02
* **やったこと**: 
  * *ユーザー入力と出力*:
    * `input()` を使ったユーザー入力の受け取りと変数への格納。
    * f文字列（`f"{変数名}"`）を使った、変数と文字列を組み合わせた画面出力（`print()`）。
  * *データ型の変換と条件分岐*
    * `int()` を使った、文字列から整数への型変換。
    * `if` / `else` を使った条件分岐ロジックの作成。
  * *リストと繰り返し処理*
    * `[ ]` を使った複数データのリスト化。
    * `append()` メソッドを使った、リスト末尾へのデータ追加。
    * `for 変数 in リスト:` を使った、リスト要素の反復処理。
    * （これらを組み合わせた「お買い物リスト作成プログラム」の実装）
* **気づき・他言語との違い**:
  * *Javaとの違い*:
    * 型とセミコロンが消滅: String name = "たま"; ではなく、単に name = "たま" と書く。行末の ; も不要。
    * カッコ {} が消滅: if や for のブロックは {} ではなく、「インデント（半角スペース4つ）」だけで表現する。インデントがずれるとエラーになる。
    * おまじないが消滅: public class... や public static void main... は一切不要。1行目からいきなり処理を書けばそのまま動く。
    * 自動で src フォルダを生成するような機能や、厳格なディレクトリ構成の強制ルールがない。
  * *ブロック（スコープ）の表現*:
    * *Java*: 中括弧 `{ }` で処理のまとまりを囲む。
    * *Python*: 中括弧は使わず、コロン `:` と **インデント（通常は半角スペース4つ）** のみで処理のまとまりを表現する。インデントがずれるとエラーになる。
  * *文字列の組み立て*
    * *Java*: `+` 演算子で文字列と変数を結合することが多い。
    * *Python*: f文字列（`f"{変数}"`）を使うのが主流。型変換を気にせず直接埋め込めるため、コードがスッキリする。
  * *リスト（配列）と繰り返し*:
    * *Java*: 配列の宣言には `{ }` を使い、リスト操作には `ArrayList` などの型の宣言が必要。
    * *Python*: `[ ]` だけで簡単にリストを作成でき、変数の型宣言も不要。
    * Pythonの `for item in list:` は、Javaの拡張for文（`for (String item : list)`）と非常に似た感覚で扱えるため馴染みやすい。

### 2026-09-03
* **やったこと**:
  * *辞書（Dictionary）と反復処理*:
    * {} とコロン : を使った辞書の作成（キーと値のペア）。
    * items() メソッドを使った、キーと値を同時に取り出す反復処理。
  * *Pythonicな記法（リスト内包表記）*:
    * [式 for 変数 in リスト if 条件] の構文を用いた、リストからのデータ抽出と加工の実装。
  * *モダンな関数定義（型ヒント）*:
    * def を使った処理の部品化（関数の定義）。
    * 引数と戻り値に型を明記する「型ヒント（Type Hinting）」の実装。（例: def func(age: int) -> str:）
  * *フレームワークと設計思想のマッピング*:
    * これまでの開発経験（PHP/Laravel, TypeScript/Next.jsなど）と、Pythonエコシステム（Django, FastAPI）の紐づけと言語化。
  * *実践演習（APIレスポンスデータを想定したフィルタリング）*:
    * filter_users.py を作成し、これまでの学習要素を組み合わせた関数を実装。
    * def get_adult_names(users: list[dict]) -> list[str]: のように、引数と戻り値に型ヒントを明記。
    * [user["name"] for user in users if user["age"] >= 20] というリスト内包表記を用いて、条件付きのデータ抽出・加工処理を1行で記述。
* **気づき・他言語との違い**:
  * *リスト内包表記（フィルタリングとマッピング）*:
    * Java/TypeScript: JavaのStream APIや、TypeScriptの filter().map() のようにメソッドをチェーンさせて書く。
    * Python: リスト内包表記を使うことで、for文のネストやメソッドチェーンを使わずに1行でスッキリと記述できる。処理速度も速く、Pythonコミュニティで最も推奨される（Pythonicな）書き方。
  * *型ヒント（Type Hinting）による安全な開発*:
    * TypeScript/Kotlin等との比較: Pythonは元々PHPやJSと同じ動的型付け言語だが、モダンな実務環境では静的型付け言語のように「型ヒント」を付けるのが主流。
    * 違い: TypeScriptのコンパイルエラーとは異なり、Pythonの型ヒントは「実行時には無視される（エラーにならない）」。しかし、エディタ（VSCode）や静的解析ツール（mypy）と組み合わせることで、未然にバグを防ぐ堅牢な開発が可能になる。
  * *辞書（Dictionary）の扱いやすさ*:
    * Java: HashMap などのクラス宣言やジェネリクス（型の指定）が必要。
    * Python: { "キー": "値" } だけで簡単に作成可能。items() を使えばキーと値の両方を一度のループで簡単に扱えるため、記述量が圧倒的に少ない。
  * *既存スキルとフレームワークの親和性*:
    * PHP（Laravel）のMVCやORM（MySQL）の概念は、Pythonのフルスタックフレームワークである Django に直訳して活かせる。
    * TypeScriptやReact Native、Swiftなどでの「フロントエンド・アプリ側から見た型安全なAPI設計」の知見は、型ヒントをベースに動作するモダンなバックエンドフレームワーク FastAPI の開発に直結する。 
  * *TypeScriptのメソッドチェーンとの比較*:
    * TypeScriptの users.filter(user => user.age >= 20).map(user => user.name) と全く同じ処理が、Pythonのリスト内包表記ではメソッドを繋ぐことなく1つの構文で完結し、非常にスッキリ書ける。
  * *型ヒントの実用性*:
    * list[dict] や list[str] といった型ヒントを書くことで、動的型付け言語であるPythonでも、他言語（JavaやTypeScript）と同じように「何を受け取って何を返すか」がひと目で分かるようになる。
  * *Pythonicな命名規則*:
    * for user in users のように「複数形のリストから単数形の変数で1つずつ取り出す」命名にすることで、直感的で英語の文章のように自然なコードになる。

### 2026-09-04
* **やったこと**:
  * *クラス定義とインスタンス化*:
    * class を用いたクラスの定義。
    * __init__（コンストラクタ）と self を用いた初期化処理と、インスタンスの生成（例: tama = Cat("たま", 3.8)）。
  * *カプセル化（プロパティとアクセス制御）*:
    * 変数名にアンダースコア2つ（__）を付ける名前修飾（Name Mangling）を利用した疑似的なカプセル化（self.__weight）。
    * @property（ゲッター）と @変数名.setter（セッター）を用いた、安全な値の取得と、不正な代入（マイナスの体重など）を防ぐバリデーションの実装。
  * *継承とオーバーライド*:
    * class Siberian(Cat): のように、カッコ内に親クラスを指定して継承を実装。
    * super().__init__() や super().speak() を使った、親クラスのコンストラクタやメソッドの呼び出し。
    * 子クラスで親クラスと同じ名前のメソッド（speak）を再定義することによるオーバーライドの実装。
  * *メソッド実行時の注意点（トラブルシューティング）*:
    * pon.speak() のようにカッコ () を忘れると、処理が実行されず「メソッドのオブジェクト情報（<bound method...>）」がそのまま返ってくる仕様の確認と修正。
* **気づき・他言語との違い**:
  * *ファイル構成とクラスルールの違い*:
    * Java: 「1ファイルにつき public クラスは1つ」「ファイル名とクラス名は完全に一致させる」という厳格な縛りがある。
    * Python: 上記の縛りがなく、関連する親クラスと子クラスを1つのファイルにまとめて身軽に記述できる（1ファイル＝1モジュールという思想）。
  * *カプセル化の思想の違い*:
    * Java: private キーワードによってコンパイラレベルで厳格にアクセスを弾く。
    * Python: アクセス修飾子が存在せず、アンダースコア（__）による命名規則で「直接触らないでほしい」という意図を示す「開発者間の紳士協定」的な文化がある。
  * *直感的なプロパティアクセス*:
    * Java: getWeight() や setWeight(4.1) のように明示的にメソッドを呼び出す。
    * Python: @property を使うことで、外部からは tama.weight = 4.1 と単なる変数への代入に見えるのに、裏ではしっかりセッターのバリデーション処理が走る。TypeScriptの get / set と非常に似た感覚で直感的に扱える。
  * *オーバーライドの仕様*:
    * Java: @Override アノテーションを付与して明示する。
    * Python: アノテーションは不要で、単に同じ名前のメソッドを定義するだけで上書きされる。スッキリ書ける反面、メソッド名をタイポすると別の新規メソッドとして扱われてしまうため注意が必要。
  * *自身のインスタンスを指すキーワード*:
    * Java: this を使い、省略できることも多い。
    * Python: クラス内のメソッドでは、必ず第一引数に self を明記する必要がある（メソッド呼び出し時には自動で渡されるため引数には書かない）。

### 2026-09-07
* **やったこと**:
  * *モダンなPython環境構築（uv と仮想環境）*:
    * システム全体のPython環境を汚さないために、最新のパッケージマネージャー uv を用いてプロジェクト単位の仮想環境を作成（uv venv）。
    * PowerShellのセキュリティ制限（ExecutionPolicy）にブロックされた際、Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned で一時的に実行権限を付与し、仮想環境を有効化（.venv\Scripts\activate）。
  * *外部API通信とセキュリティブロックの回避*:
    * uv pip install requests で外部ライブラリを導入し、requests.get() によるAPI通信を実装。
    * プログラムからの通信がサーバーに弾かれるエラー（ConnectionResetError 10054）に対し、ヘッダーに User-Agent（ブラウザからのアクセスを装う情報）を付与することでブロックを回避。
  * *「黄金の3ステップ」の実装（API取得 → オブジェクト化 → フィルタリング）*:
    * 1. データ取得: JSONPlaceholderからユーザー一覧の生データ（辞書のリスト）を取得。
    * 2. オブジェクト化: User クラスを定義し、リスト内包表記を用いて [User(user["name"], user["company"]["name"]) for user in raw_users] のように、生の辞書をクラスのインスタンス（オブジェクト）に一括変換。
    * 3. ビジネスロジック: 再度リスト内包表記を用い、[u for u in user_objects if "Group" in u.company_name] として特定の条件を満たすオブジェクトのみを抽出。
* **気づき・他言語との違い**:
  * *モダンなパッケージ管理*:Pythonにおける uv は、TypeScript界隈における pnpm や bun のような位置づけ。PEP 668（グローバル環境へのインストール制限）の保護機構が働いていることを実体験し、Pythonでも仮想環境（Node.jsの node_modules に相当）をプロジェクトごとに切るのが必須であることを学んだ。
  * *オブジェクト指向の真価（辞書 vs クラス）*:
    * 生のJSON（辞書）のままだと user["company"]["name"] のようにキーを文字列で指定し続ける必要があり、タイポによるバグが起きやすい。
    * 一度クラスにマッピングしてしまえば、member.company_name のようにプロパティとして安全かつ直感的にアクセスできる。さらに introduce() のような振る舞い（メソッド）も持たせることができる。
  * *バックエンド開発への直結*:今回実装した「生のレスポンス・DBの値をオブジェクトに変換し、加工して返す」という一連のフローは、Laravel（PHP）におけるORMのModel操作や、Next.js（TypeScript）におけるAPIレスポンスの型変換（DTO）と全く同じ概念。Pythonのリスト内包表記を使うことで、これらを極めて簡潔に記述できることが確認できた。

### 2026-09-09
* **やったこと**:
  * *システム要件定義とモデリング*:
    * 社員管理システム（MVP）に必要な項目を洗い出し、最小限のデータ構成（社員番号、名前、役職、メールアドレス）を定義。
  * *実行環境のエラー切り分け*:
    * uv 環境で FastAPI のサーバー起動を試みたが、会社のセキュリティポリシー（AppLockerによる AppData 配下のDLL実行制限）により弾かれる事象が発生。エラーログから環境要因であることを特定し、社内へ確認・相談を実施。
  * *ローカルDB（SQLite）の構築*:
    * サーバー起動を一旦保留し、Python標準の sqlite3 を用いてオフラインで動くデータベース（company.db）を構築する db_setup.py を作成。
  * データベースへの接続、CREATE TABLE によるテーブル作成、および INSERT OR IGNORE によるモックデータ（テスト太郎、山田花子）の保存を実装（conn.commit() による変更の確定）。
  * *モダンなクラス定義*:
    * @dataclass デコレータを導入し、__init__ メソッドを省略した簡潔な Employee クラスを定義。
* **気づき・他言語との違い**:
  * *DB設計の鉄則（不変の事実を保存する）*:
    * 「年齢」や「勤続年数」など毎日変動する値はデータベースに保存せず、「生年月日」や「入社日」といった変わらない事実のみを保存し、必要に応じてプログラム側で計算するのが実務のベストプラクティスであると学んだ。
  * *Pythonのモダンなクラス（@dataclass）*:
    * @dataclass を使うと、TypeScriptの interface や type 定義に近い感覚で、直感的かつ非常に少ない記述量でデータの「型」と「構造」を定義できることがわかった。
  * *エラー原因の切り分けスキル*:
    * エラーが出た際、「自分のコードのバグ」なのか「PCの環境制限（セキュリティ等）」なのかをエラーログのパス（C:\Users\...\AppData\...）から読み取り、切り分ける良い実務訓練になった。
  * *Pythonの基礎構文の落とし穴*:
    * メソッドの実行時はカッコを関数名の直後に置く（execute("...")）、オブジェクトに対する操作はドットで繋ぐ（conn.commit()）など、基礎的な文法ルールを再確認できた。

### 2026-09-10
* **やったこと**:
  * *環境の再構築と限界突破の検証*:
    * AppLockerの制限を回避するため、Cドライブ直下（C:\dev）にプロジェクトを移動し、仮想環境の再構築とサーバー起動を検証。
    * 結果としてOSレベルの制御（ツール裏側でのAppData参照ブロック）であることを明確に特定し、情シスへの具体的なエスカレーション（ホワイトリスト登録または権限付与の依頼）を実施した。
  * *SQLiteでの検索機能（Read）の実装*:
    * db_setup.py にて、LIKE 演算子を用いた部分一致検索機能を実装。
    * プレースホルダー（?）を使用し、実務水準のセキュリティ（SQLインジェクション対策）を担保した状態でデータ取得を行った。
    * 検索結果の生データ（タプル）を Employee オブジェクトへマッピングし、一覧として出力する処理を完成させた
* **気づき・他言語との違い**:
* *開発環境のベストプラクティス*:
    * デスクトップ等のユーザーフォルダはパスが長くなり同期トラブルも起きやすいため、Windowsでの開発拠点は C:\dev のような直下ディレクトリが実務の基本であることを学んだ。
  * *冪等性（べきとうせい）の重要性*:
    * INSERT OR IGNORE や CREATE TABLE IF NOT EXISTS を使うことで、「実行場所が変わっても・何度実行してもエラーにならない（冪等性が保たれる）」安全なスクリプト設計の恩恵を肌で感じた。

### 2026-09-11
* **やったこと**:
  * *CLI版CRUDアプリケーションの構築*:
    * Pythonの標準ライブラリ sqlite3 を用い、ターミナル上で動作する対話型の社員管理システムをゼロから作成。
  * *CRUD操作の完全実装*:
    * データの登録(Create)、一覧/検索(Read)、更新(Update)、削除(Delete)の全機能を網羅。
  * *セキュアなデータベース操作*:
    * SQL文に直接変数を埋め込まず、プレースホルダー（?）を活用することで、SQLインジェクション攻撃を防止。
  * *実務を想定したバリデーション*:
    * 更新・削除処理の直前に SELECT ＋ fetchone() による対象IDの存在チェックを実行。該当データがない場合はループを継続（continue）させる防御的プログラミングを実装。
  * *CLI特有のUX改善*:
    * input() を活用した画面スクロールのストッパー機能や、.strip() による見えない空白文字の除去など、入力エラーを未然に防ぐ処理を追加。
* **気づき・他言語との違い**:
* *SQLの厳密さ*:
  * VALUE と VALUES（複数形）の違いや、UPDATE/DELETE 時の WHERE 句の指定など、少しの構文ミスがシステムエラーやデータ喪失（全件削除など）に直結することを実感した。
* *処理速度とUXの乖離*:
  * プログラムの実行速度は人間の目より遥かに早いため、適切なタイミングで処理を一時停止させないと、「画面が消えたように見える」などのUX低下を引き起こす。
* *入力値のブレを吸収する設計*:
  * ユーザーの入力は常に正しいとは限らない（全角・半角の違い、誤字など）。システム側で .strip() やリスト判定（in ["1", "１"]）を用いてブレを吸収する設計が重要。
* *データベース接続の容易さ*:
  * Java（JDBCドライバの準備が必要）やPHP（PDOなどの設定が必要）と異なり、Pythonは標準で内蔵されている sqlite3 をインポートするだけで、即座にローカルDB環境を構築できる軽量さが際立っている。
* *データモデルの定義（クラス）*:
  * TypeScriptの interface やJavaのカプセル化（ゲッター/セッター）に比べ、Pythonの @dataclass は型ヒントを書き並べるだけでデータクラスが完成するため、記述量が圧倒的に少なく可読性が高い。
* *コードの構造とブロック*:
  PHPやSwiftなどで用いる波括弧 { } によるブロック分けが存在せず、Pythonはインデント（字下げ）そのものが文法ルールに組み込まれているため、ネストが深くなっても視覚的に処理のまとまり（while や if の範囲）が追いやすい。

### 2026-09-14
* **やったこと**:
  * *空文字入力のブロック（バリデーション）*:
    * 新規登録時に if new_name == "" or new_email == "": という条件を追加し、空文字（透明人間）の登録を事前に弾く仕組みを実装。
  * *データベースへの UNIQUE 制約追加*:
    * email カラムに UNIQUE 制約を付与した新しいデータベース（company_v2.db）を作成し、同一メールアドレスの重複登録をデータベース側で完全にブロック。
  * *try-except による例外処理の実装*:
    * 重複エラー発生時の強制終了を防ぐため、登録処理（INSERT）を try ブロックで囲み、sqlite3.IntegrityError を except でキャッチして、ユーザーに優しいエラーメッセージを表示するよう改修（UXの向上）。
  * *FastAPI（Web API）移行に向けた概念整理*:
    * 現在のCLIアプリの構成要素（while True、input()、print()）が、Web API（サーバー、HTTPメソッド、JSONレスポンス）にどう置き換わるかの対応表を作成し、コアとなるSQL処理はそのまま流用できることを確認。
* **気づき・他言語との違い**:
  * *文字列比較の罠（is と == の違い）*:
    * Pythonにおいて、文字列の「中身」が同じかを判定する場合は == を使用する。is は「メモリ上の場所（オブジェクトID）」を比較するため、意図しない挙動のすり抜けを生む原因になることを学んだ。
  * *例外処理（エラーハンドリング）の重要性*:
    * データベースが制約（UNIQUE等）で異常を弾いてくれても、プログラム側でそのエラー（例外）をキャッチしなければシステム全体がクラッシュしてしまう。実務では try-except を用いて「想定されるエラーを安全に処理し、システムを止めない設計」が不可欠であると体感した。
  * *エラー名の正確な指定*:
    * except で指定するエラー名（例：IntegrityError と InternalError のタイポ）が1文字でも異なると、対象のエラーをキャッチできず素通りしてしまうため、エラーログを正確に読み解くことがデバッグの鍵となる。
  * *Python特有の省略記法*:
    * Pythonでは「空文字（""）は False として扱われる」という特性があるため、if new_name == "": を if not new_name: と省略して書く「Pythonic（パイソニック）」な記法が実務でよく使われる点（厳格な型チェックを行うJava等との違い）。

### 2026-09-15
* **やったこと**:
  * *FastAPIローカル開発環境の構築と起動*:
    * Windows特有のPythonエイリアス（ストアアプリ起動設定）による仮想環境の破損問題を、高速環境管理ツール uv を導入することで回避・解決した。
    * PowerShellの実行ポリシー（ExecutionPolicy）がスクリプト実行を制限していたため、管理者権限を用いて現在のユーザーに RemoteSigned を付与し、仮想環境の有効化（activate）を可能にした。
    * fastapi および uvicorn をインストールし、モックデータを持つ main.py を起動。ブラウザから [http://127.0.0.1:8000/employees](http://127.0.0.1:8000/employees) にアクセスし、想定通りのJSONレスポンスを得ることに成功した。
  * *CRUD APIの完全実装（FastAPI × SQLite）*:
    * GET, POST, PUT, DELETE メソッドを用い、DBと連動した社員情報の操作エンドポイントを構築。
    * データの新規登録時には一意制約（UNIQUE）違反をハンドリングし、適切なエラーレスポンスを返却。
    * 更新・削除処理では cursor.rowcount を評価することで、不要な事前クエリ（SELECT）を省きつつ、対象データが存在しない場合のエラー処理を実装。
    * FastAPI標準の Swagger UI (/docs) を用いたリクエスト・レスポンスの疎通テストを実施。
* **気づき・他言語との違い**:
  * *環境構築におけるOS仕様への理解の重要性*:
    * チュートリアル通りにコマンドを打てば動くわけではなく、実務環境（Windows）ではOSのお節介機能（ダミーのpython.exe）やセキュリティ制限（PowerShellの実行ポリシー）が壁になることを実体験として学んだ。エラーログから根本原因を特定してアプローチする力がついた。
  * *他言語（PHP/LaravelやTypeScript）の環境構築との違い*:
    * Node.js（npm）やPHP（Composer）では、OS標準機能がコマンドを横取りして環境を破壊するケースは少ないが、Pythonの標準モジュール（venv）はWindowsの仕様と衝突しやすい。一方で、Pythonにおける uv のような代替ツールを活用すれば、Docker等を使わずとも一瞬でクリーンな隔離環境を作れる手軽さは非常に魅力的だと感じた。
  * *型安全とバリデーションの手軽さ*:
    * Pythonは動的型付けのイメージが強かったが、FastAPIではPydanticの BaseModel を使うことで、TypeScriptのインターフェース定義に近い感覚で型チェックと入力バリデーションを実現できる点が非常にスマートだと感じた。

### 2026-09-16
* **やったこと**:
  * *API機能のモジュール化（リファクタリング）*:
    * 保守性を高めるため、アプリケーションを main.py（ルーティング）、database.py（DB操作）、models.py（型定義）の3層に分割。
    * 各エンドポイントはビジネスロジックを直接持たず、データベース操作関数を呼び出すのみのクリーンな設計に変更。
    * ターミナルのカレントディレクトリと実行パスの概念を整理し、ローカル環境での正しい起動手順を確立。
  * *RPA連携を想定したバッチ処理（ファイル操作）の実装*:
    * セキュリティ制約（アプリケーション制御ポリシーによるpandas等のDLLブロック）を回避するため、外部ライブラリに依存せず標準ライブラリ（csv, pathlib, shutil）のみで堅牢なデータ加工処理を実装。
    * 複数CSVの読み込み、欠損値の補完、特定条件のデータ除外、辞書（dict）を用いた社員マスタとのVLOOKUP的結合処理を構築。
    * 相対パス指定におけるカレントディレクトリの概念を復習し、FileNotFoundError の原因特定と解決を自力で実施。
* **気づき・他言語との違い**:
  * *バックエンドとしてのPythonの立ち位置*:
    * Web開発におけるPython（FastAPI）は、HTMLを直接描画するのではなく、フロントエンド（Next.jsやReact Nativeなど）にJSONを提供する純粋なバックエンドAPIサーバーとして機能することを体感した。
  * *モジュール間の依存関係管理*:
    * Python特有のファイル間のインポートにおいて、型情報（Pydanticモデル）をルーティング側（main.py）とDB操作側（database.py）の両方で正しく読み込む必要があるなど、モジュール化における依存関係の基本ルールを学んだ。
  * *制約下における標準ライブラリの強力さ*:
    * フレームワークや外部ライブラリ（pandas等）が使えない環境でも、Python標準のモジュールを組み合わせることで、実務レベルの高速なデータクレンジングとファイル整理（作成・移動）が十分に可能であることを体感した。
  * *運用を見据えた防衛的プログラミングの作法*:
    * ファイルの開きっぱなしを防ぎロックを解除する with open(...) 構文や、モダンなパス操作を行う pathlib の重要性を学習。
    * 特にWindows環境特有の文字コード問題（cp932とutf-8の衝突）を防ぐためのエンコーディング指定や、ファイル不在を想定した例外処理など、自動化スクリプトを安全に稼働させ続けるための必須作法を学んだ。

### 2026-09-17
* **やったこと**:
  * *RPA連携を見据えたバッチ処理のCLI化とログ出力の実装*:
    * argparseモジュールを導入し、入力ファイルと出力ファイルのパスをコマンドライン引数（--input, --output）として動的に受け取る仕組みを構築。ファイル名をハードコードしない汎用的なツールへと進化させた。
    * loggingモジュールを用いて、処理の開始・完了、各ステップの進捗、エラー発生時の詳細を app.log に追記形式で記録する運用保守の仕組みを実装。
    * 前日に実装したCSVクレンジングおよびマスタ結合のロジックと統合し、実務でそのまま稼働できるレベルの堅牢な完全版スクリプトを完成させた。
* **気づき・他言語との違い**:
  * *f文字列（f-string）の直感的な操作性*:
    * 変数を文字列に埋め込む際、Pythonのf文字列（f"..."）はTypeScriptのテンプレートリテラル（`${...}`）等と非常に似ており、エラーメッセージ内に変数の値や例外（Exception）の生データを簡潔に埋め込めるため、ログ出力と非常に相性が良いことを学んだ。
  * *モジュールのメソッド呼び出しと辞書の文法の違い*:
    * 辞書のデータアクセス（row["Amount"]）と、モジュールの機能呼び出し（parser.add_argument() や logging.info()）の文法的な違いを明確に区別できるようになった。
  * *ログの「追記」というデフォルト仕様*:
    * 通常のファイル書き込み（mode="w"）が上書きであるのに対し、logging機能はデフォルトで「追記（アペンド）」される仕様になっており、過去の実行履歴を消さずに保持するというシステム運用上の配慮が言語レベルでなされていることに気づいた。

### 2026-09-18
* **やったこと**:
  * 